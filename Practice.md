# Practice — עבודה עם Git ו־GitHub

סיכום מעשי של כל פעולות השמירה והעדכון שבוצעו בריפו הזה, לפי הסדר שבו קרו.
כל פקודה כאן **הורצה בפועל** בפרויקט הדאשבורד — זה לא תיעוד תיאורטי.

---

## מושגי יסוד — מה ההבדל

לפני הפקודות, שלושת המושגים שמבלבלים הכי הרבה:

| מושג | מה זה | איפה זה נמצא |
|-------|--------|---------------|
| **commit** | תמונת מצב שמורה של הקבצים | במחשב שלך בלבד |
| **push** | שליחת הקומיטים לגיטהאב | מעביר מהמחשב לענן |
| **branch** | קו עבודה מקביל | יכול להיות בשני המקומות |

**נקודה קריטית:** `commit` לא שולח שום דבר לגיטהאב. אפשר לעשות עשרה קומיטים ועדיין שום דבר לא יופיע באתר עד ש־`push` ירוץ.

---

## 1. בדיקת מצב — תמיד מתחילים מכאן

```bash
git status
```

מראה מה השתנה ומה עדיין לא נשמר. שלושה מצבים אפשריים לכל קובץ:

- `??` — קובץ חדש שגיט לא מכיר (untracked)
- ` M` — קובץ קיים שהשתנה (modified)
- `A ` / `M ` — כבר נוסף לקומיט הבא (staged)

גרסה מקוצרת וקריאה יותר:

```bash
git status --short
```

לראות את ההיסטוריה:

```bash
git log --oneline
```

---

## 2. שמירה — add ו־commit

`add` בוחר מה ייכנס לקומיט, `commit` שומר אותו בפועל.

```bash
git add repo-1
```

```bash
git commit -m "Add factory machines production dashboard"
```

להוסיף את כל השינויים בבת אחת:

```bash
git add -A
```

### הודעת קומיט טובה

ההודעה נשארת בהיסטוריה לנצח. מה שעבד טוב בפרויקט הזה:

- שורה ראשונה קצרה, עד ~50 תווים, בלשון ציווי: `Add dashboard`, לא `Added` ולא `Adding`
- שורה ריקה
- פירוט — **למה** השינוי נעשה, לא רק מה השתנה

דוגמה אמיתית מהריפו הזה:

```
Make the dashboard actually run, and verify it end to end

Replace better-sqlite3 with the built-in node:sqlite module. The native
package has no prebuilt binary for Node 24, and npm 11 blocks install
scripts by default, so it could not build.
```

---

## 3. שליחה לגיטהאב — push

```bash
git push origin main
```

`origin` = הכינוי של הריפו בגיטהאב. `main` = הענף שנשלח.

לראות לאן `origin` מצביע:

```bash
git remote -v
```

---

## 4. ענפים — branch

ענף הוא קו עבודה נפרד. עובדים בו בלי לגעת ב־`main`, וממזגים כשמוכן.

```bash
git checkout -b feat/factory-dashboard
```

יוצר ענף חדש **ועובר אליו** בפקודה אחת. מעבר בין ענפים קיימים:

```bash
git checkout main
```

רשימת ענפים (הכוכבית מסמנת איפה אתה נמצא):

```bash
git branch -a
```

שליחת ענף חדש לגיטהאב בפעם הראשונה:

```bash
git push -u origin feat/factory-dashboard
```

ה־`-u` קושר בין הענף המקומי לענף בגיטהאב, כך שבפעמים הבאות מספיק `git push`.

### ⚠️ מלכודת אמיתית שנתקלנו בה

`git checkout` **מחליף את הקבצים בתיקייה**. כשעברנו מהענף ל־`main`, כל קבצי הפרויקט נמחקו לרגע — כי `main` עדיין לא הכיל אותם — וחזרו שנייה אחר כך אחרי המיזוג.

שרת הפיתוח שרץ ברקע שם לב שקובץ הקונפיגורציה "נעלם", הפעיל את עצמו מחדש בלי הקונפיגורציה, והדאשבורד התחיל להחזיר שגיאה:

```
Unexpected token '<', "<!doctype "... is not valid JSON
```

**המסקנה:** אחרי `git checkout` שמשנה הרבה קבצים — תעצור ותפעיל מחדש כל שרת שרץ ברקע.

---

## 5. מיזוג — merge

להחזיר את העבודה מהענף אל `main`:

```bash
git checkout main
```

```bash
git merge --ff-only feat/factory-dashboard
```

`--ff-only` (fast-forward) מזיז את `main` קדימה **בלי ליצור קומיט מיזוג מיותר**. ההיסטוריה נשארת שורה ישרה ונקייה. אם זה לא אפשרי — הפקודה נכשלת במקום לייצר בלגן, וזו התנהגות רצויה.

מחיקת הענף אחרי שסיים את תפקידו:

```bash
git branch -d feat/factory-dashboard
```

```bash
git push origin --delete feat/factory-dashboard
```

מחיקת ענף בגיטהאב **סוגרת אוטומטית** PR פתוח שהצביע עליו ומסמנת אותו כ־merged.

---

## 6. Pull Request — עם gh

`gh` הוא כלי שורת הפקודה של גיטהאב. בדיקה שאתה מחובר:

```bash
gh auth status
```

פתיחת PR:

```bash
gh pr create --base main --head feat/factory-dashboard --title "כותרת"
```

צפייה במצב:

```bash
gh pr view 1
```

### מתי PR ומתי לא

| מצב | המלצה |
|------|--------|
| עובד לבד, פרויקט קטן | ישר ל־`main`, בלי ענף |
| קוד שלא נבדק / שינוי גדול | ענף + PR |
| צוות | תמיד PR |

בפרויקט הזה נפתח PR כי הקוד עדיין לא הורץ — ואחר כך, לבקשתך, הכל הועבר ישירות ל־`main` והענף נמחק.

---

## 7. ניהול הריפו — gh repo

מצב נוכחי:

```bash
gh repo view
```

הפיכת ריפו פרטי לציבורי:

```bash
gh repo edit --visibility public --accept-visibility-change-consequences
```

> ⚠️ **פעולה שקשה לבטל.** ברגע שריפו הופך לציבורי, כל ההיסטוריה שלו נגישה לכל אחד — כולל קבצים שנמחקו בקומיטים קודמים. מחיקה מאוחרת של קובץ לא מסירה אותו מההיסטוריה. לפני שהופכים ריפו לציבורי: לוודא שאין בו סיסמאות, מפתחות API או נתונים עסקיים.

---

## 8. .gitignore — מה לא נשמר

קבצים שאסור שייכנסו לגיט: תיקיות ענק, קבצים שנוצרים אוטומטית, וכל דבר רגיש.

ה־`.gitignore` של הפרויקט הזה:

```
node_modules/     # עשרות אלפי קבצים, מותקנים מחדש עם npm install
dist/             # תוצר בנייה, נוצר מחדש בכל build
*.db              # מסד הנתונים המקומי
.env              # משתני סביבה — כאן יושבות סיסמאות
```

**כן שומרים** את `package-lock.json` — הוא נועל גרסאות מדויקות של החבילות, כך שאותו קוד ייבנה אותו דבר בכל מחשב.

בדיקה שקובץ באמת מוחרג:

```bash
git check-ignore -v repo-1/server/factory.db
```

---

## 9. GitHub Pages — אתר אוטומטי מהריפו

Pages מארח **קבצים סטטיים בלבד**: HTML, CSS, JS. אין שם Node, אין שרת, אין מסד נתונים.

הפעלה:

```bash
gh api -X POST repos/Hamoudehh/claude-code-learn/pages -f build_type=workflow
```

הכתובת שנוצרת: `https://<שם-משתמש>.github.io/<שם-ריפו>/`

בפרויקט הזה: **https://hamoudehh.github.io/claude-code-learn/**

### שלוש התאמות שהיו הכרחיות

| בעיה | פתרון |
|-------|--------|
| האתר יושב בתת־נתיב `/claude-code-learn/` ולא בשורש | הגדרת `base` בבנייה |
| Pages לא יודע להגיש נתיבי SPA — רענון על `/compare` מחזיר 404 | מעבר לניתוב מבוסס `#` |
| אין שרת שיספק נתונים | שכבת נתונים חלופית בדפדפן, נבחרת בזמן הבנייה |

---

## 10. פריסה אוטומטית — GitHub Actions

הקובץ [.github/workflows/deploy-pages.yml](.github/workflows/deploy-pages.yml) מריץ בנייה ופריסה **בכל דחיפה ל־`main`**. מאז שהוא קיים, `git push` מספיק כדי לעדכן את האתר החי.

המבנה:

```yaml
on:
  push:
    branches: [main]    # מתי לרוץ
```

מעקב אחרי ריצות:

```bash
gh run list --limit 5
```

```bash
gh run watch
```

צפייה בשגיאות של ריצה שנכשלה:

```bash
gh run view --log-failed
```

---

## 11. מחזור העבודה היומיומי

אחרי שהכל מוגדר, זה כל מה שצריך:

```bash
git status
```

```bash
git add -A
```

```bash
git commit -m "תיאור השינוי"
```

```bash
git push
```

והאתר מתעדכן לבד תוך כדקה.

---

## תקלות שנתקלנו בהן בפועל

| מה קרה | הסיבה | הפתרון |
|---------|--------|---------|
| `LF will be replaced by CRLF` | Windows ו־Linux משתמשים בתווי סוף־שורה שונים | **אזהרה בלבד, לא שגיאה.** אפשר להתעלם |
| הדאשבורד החזיר HTML במקום נתונים | שרת שרץ ברקע נשבר אחרי `git checkout` | להפעיל מחדש את השרת |
| טקסט עברי הפך לג'יבריש | עריכת קובץ דרך PowerShell שקרא UTF-8 כ־ANSI | לא לערוך קבצי UTF-8 עם `Get-Content` + `Set-Content` |
| `gh pr view --json merged` נכשל | שם שדה שגוי | `gh pr view --json` בלי ערך מדפיס את כל השדות האפשריים |

---

## פקודות חירום

**ביטול שינוי בקובץ שעוד לא נשמר:**

```bash
git checkout -- <שם-הקובץ>
```

**לראות מה בדיוק השתנה לפני שמירה:**

```bash
git diff
```

**ביטול הקומיט האחרון, אבל שמירת השינויים בקבצים:**

```bash
git reset --soft HEAD~1
```

> `git reset --hard` מוחק שינויים **ללא אפשרות שחזור**. להשתמש רק כשבטוחים לגמרי.
